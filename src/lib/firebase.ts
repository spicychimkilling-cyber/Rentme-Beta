// Firebase removed: this file is intentionally left as a stub.
// The project now uses Appwrite for database and storage.

// If you still need to reference legacy Firebase symbols, update imports
// to use Appwrite equivalents in `src/lib/appwrite.ts` instead.

export {}; 
